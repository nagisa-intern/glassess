<%= form_with(model: comic_datum, local: true) do |form| %>
  <% if comic_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comic_datum.errors.count, "error") %> prohibited this comic_datum from being saved:</h2>

      <ul>
      <% comic_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :episode %>
    <%= form.number_field :episode %>
  </div>

  <div class="field">
    <%= form.label :comic_id %>
    <%= form.text_field :comic_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
